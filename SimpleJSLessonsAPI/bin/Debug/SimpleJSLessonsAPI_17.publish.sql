/*
Deployment script for SimpleJSLessonsAPIData

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "SimpleJSLessonsAPIData"
:setvar DefaultFilePrefix "SimpleJSLessonsAPIData"
:setvar DefaultDataPath "C:\Users\David Nguyen\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\David Nguyen\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[sessionModel].[sessionID] is being dropped, data loss could occur.

The column [dbo].[SessionModel].[sessionHash] on table [dbo].[SessionModel] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[SessionModel])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Dropping [dbo].[fk_usersession_to_user]...';


GO
ALTER TABLE [dbo].[sessionModel] DROP CONSTRAINT [fk_usersession_to_user];


GO
PRINT N'Dropping unnamed constraint on [dbo].[sessionModel]...';


GO
ALTER TABLE [dbo].[sessionModel] DROP CONSTRAINT [UQ__sessionM__23DB12CAC82C184A];


GO
PRINT N'Starting rebuilding table [dbo].[SessionModel]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_SessionModel] (
    [Id]          INT          NOT NULL,
    [sessionHash] VARCHAR (64) NOT NULL,
    [accountHash] VARCHAR (64) NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[sessionModel])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_SessionModel] ([Id], [accountHash])
        SELECT   [Id],
                 [accountHash]
        FROM     [dbo].[sessionModel]
        ORDER BY [Id] ASC;
    END

DROP TABLE [dbo].[sessionModel];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_SessionModel]', N'SessionModel';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Creating [dbo].[UserSavedDemos]...';


GO
CREATE TABLE [dbo].[UserSavedDemos] (
    [ID]          INT           IDENTITY (1, 1) NOT NULL,
    [accountHash] VARCHAR (64)  NOT NULL,
    [demoHash]    VARCHAR (24)  NOT NULL,
    [demoTitle]   VARCHAR (128) NULL,
    PRIMARY KEY CLUSTERED ([ID] ASC)
);


GO
PRINT N'Creating [dbo].[fk_demoHash_to_user]...';


GO
ALTER TABLE [dbo].[UserSavedDemos] WITH NOCHECK
    ADD CONSTRAINT [fk_demoHash_to_user] FOREIGN KEY ([accountHash]) REFERENCES [dbo].[apiUser] ([accountHash]);


GO
PRINT N'Checking existing data against newly created constraints';


GO
USE [$(DatabaseName)];


GO
ALTER TABLE [dbo].[UserSavedDemos] WITH CHECK CHECK CONSTRAINT [fk_demoHash_to_user];


GO
PRINT N'Update complete.';


GO
